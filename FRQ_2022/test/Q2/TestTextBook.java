package Q2;

import org.junit.Assert;
import org.junit.Test;

public class TestTextBook {
    /* Uncommment these tests when you have a solution for Q2.
    @Test
    public void testConstructor() {
        TextBook textBook = new TextBook("Astronomy", 49.75, 2);
        Assert.assertEquals("Astronomy", textBook.getTitle());
    }

    @Test
    public void testGetBookInfo() {
        TextBook textBook = new TextBook("Astronomy", 15.5, 3);
        Assert.assertEquals("Astronomy-15.5-3", textBook.getBookInfo());
    }

    @Test
    public void testCanSubstituteForSameTitle() {
        TextBook bookOne = new TextBook("Astronomy", 15.5, 1);
        TextBook bookTwo = new TextBook("Astronomy", 15, 5);
        Assert.assertFalse(bookOne.canSubstituteFor(bookTwo));
        Assert.assertTrue(bookTwo.canSubstituteFor(bookOne));
    }

    @Test
    public void testCanSubstituteForDifferentTitle() {
        TextBook bookOne = new TextBook("Astronomy", 15.5, 1);
        TextBook bookTwo = new TextBook("Gym", 15, 5);
        Assert.assertFalse(bookOne.canSubstituteFor(bookTwo));
        Assert.assertFalse(bookTwo.canSubstituteFor(bookOne));
    }
     */
}
